[project]
name = "cheapchocolate"
version = "0.1.0"
description = "CheapChocolate is an simple imap client to receive you daily email."
readme = "README.md"
authors = [
    { name = "Cesar Cardoso", email = "hello@cesarcardoso.cc" }
]
requires-python = ">=3.11"
dependencies = [
    "dotenv>=0.9.9",
]


[project.scripts]
ohmyscrapper = "cheapchocolate:main"


[tool.bumpversion]
tag = true
tag_name = "v{new_version}"
pre_commit_hooks = ["uv sync --upgrade", "git add uv.lock"]
commit = true

[[tool.bumpversion.files]]
filename = "pyproject.toml"

[[tool.bumpversion.files]]
filename = "README.md"

[[tool.bumpversion.files]]
filename = "src/cheapchocolate/__init__.py"
